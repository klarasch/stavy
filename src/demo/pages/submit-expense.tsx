import type { PageProps } from "@/stavy/types"
import { FormFlowTemplate } from "../templates/FormFlowTemplate"

export default function SubmitExpensePage(props: PageProps) {
  return <FormFlowTemplate {...props} />
}
